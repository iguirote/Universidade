Maze Runner / Labirinto

(----------->Cenario<-----------)
	Matriz [n,n] (char)

1) Defina obstáculos em células da matriz de forma aleatória (% definida pelo usuário)
2) Sortear a posição linha, coluna "E"
3) Sortear a posição linha, coluna "S"


PROBLEMA/OBJETIVO
|
+----->	Descobrir um caminho entre 'E' e 'S'
|
+-----> Relatar os comandos

(O melhor caminho seria o mais curto, e pra saber qual só testando TODOS)


(----------->Estados<-----------) 
=  estado contendo : 
-> Matriz [n,n] - char
	|
	+----->'E' - entrada
	|
	+----->'S' - saida
	|
	+----->'@' - Obstaculo
-> LinhaEntrada - Int
-> ColunaEntrada - Int
-> LinhaSaida - Int
-> ColunaSaida - Int

<---ESTADO INICIAL--->
 	(E0)

E: Sorteados em linhas e colunas VÁLIDOS
S: Sorteados em linhas e colunas VÁLIDOS
@: Sorteados em linhas e colunas VÁLIDOS


<---ESTADO FINAL--->
 	(Ef)

if(LinhaEntrada == LinhaSaida 
	    &&
ColunaEntrada == ColunaSaida) {
//Resto do código....
}

(----------->REGRAS DE TRANSIÇÃO<-----------)
(COM SUAS RESPECTIVAS RESTRIÇÕES)

CIMA : LinhaEntrada--
BAIXO : LinhaEntrada++ 
PT : ColunaEntrada--
DIREITA : ColunaEntrada++
	
(RESTRIÇÃO é no caso para não ir mais casas do que a matriz)

if (LinhaEntrada == N-1) {//Impede de ir mais pra baixo do que a matriz permite}
if (LinhaEntrada == 0) {//Impede de ir mais pra cima do que a matriz permite}


(----------->VISITADOS<-----------)

String da concatenação da LinhaEntrada com ColunaEntrada

(----------->Função Objetiva<-----------)

'E' na mesma célula que 'S' (O estado final)